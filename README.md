# TasksApp (Lista de Tareas)

Un proyecto frontend completo y funcional para una aplicación de lista de tareas (To-Do list). Este proyecto incluye una página de inicio (Landing Page) y la aplicación principal capaz de gestionar, guardar y manipular tareas dinámicamente.

El proyecto está construido con **HTML5**, **CSS3** moderno y **JavaScript (Vanilla JS)**, enfocado en un diseño limpio, responsivo y con una experiencia de usuario fluida.

## 🚀 Características Principales

* **Página de Inicio:** Una landing page de bienvenida (`index.html`) que introduce la aplicación y enlaza a la herramienta principal.
* **Gestión de Tareas (Funcional):** Gracias a la integración de JavaScript (`app.js`), la aplicación permite:
    * **Crear Tareas:** Captura de datos desde el formulario y renderizado dinámico en el DOM.
    * **Eliminar Tareas:** Funcionalidad para borrar ítems específicos de la lista.
    * **Interactividad:** Uso de *Event Listeners* para una respuesta inmediata a las acciones del usuario.
* **Persistencia de Datos:** Uso de **LocalStorage** para que las tareas no se pierdan al recargar la página o cerrar el navegador.
* **Validación de Datos:** Lógica de control que impide la creación de tareas vacías y muestra/oculta mensajes de error dinámicamente.
* **Tema Claro y Oscuro:** Utiliza variables CSS y la función `light-dark()` para adaptarse automáticamente al tema del sistema operativo del usuario.
* **Diseño Responsivo:** La interfaz se adapta fluidamente a pantallas de escritorio, tablets y móviles usando Flexbox y Media Queries.

## 🛠️ Tecnologías Utilizadas

* **HTML5:** Estructura semántica.
* **CSS3:** Variables CSS (:root), Flexbox, Diseño Responsivo.
* **JavaScript (ES6+):** Manipulación del DOM, manejo de Arrays (`push`, `splice`, `forEach`) y almacenamiento local (`JSON.parse/stringify`).

## 🔗 PAGINA WEB
[https://github.com/Byte21m/Lista-de-tareas---Javascript](https://byte21m.github.io/Lista-de-tareas---Javascript/)
